name: "Hello World Workflow_4"

on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print hello manas"
        run: echo "Hello,world!"