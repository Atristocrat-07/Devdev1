name: "Hello World Workflow_3"

on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print My name Manasdeep"
        run: echo "Hello My friend !!"
