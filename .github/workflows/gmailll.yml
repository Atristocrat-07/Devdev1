name: "Hello World Workflow_1"

on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print hello Manasdeep"
        run: echo "Hello,world!"
