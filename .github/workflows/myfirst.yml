name: "Hello World Workflow"

on:
  push:
    branches:
      - master
      - production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: "Print hello manas"
        run: echo "This is my world!"